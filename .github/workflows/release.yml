name: Build and Release

# Triggers when you push a tag like v3.0, v3.1, etc.
on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  build:
    runs-on: macos-15
    steps:
      # 1. Check out the code
      - name: Checkout
        uses: actions/checkout@v5

      # 2. Select Xcode version
      - name: Select Xcode
        run: sudo xcode-select -s /Applications/Xcode_16.3.app/Contents/Developer

      # 3. Build the executable
      - name: Build
        run: swift build -c release

      # 4. Create .app bundle and ad-hoc sign
      - name: Package
        run: |
          APP="RetroForge.app/Contents"
          mkdir -p "$APP/MacOS" "$APP/Resources"
          cp .build/release/CHDMAN "$APP/MacOS/RetroForge"
          cp Sources/CHDMAN/Resources/Info.plist "$APP/Info.plist"
          cp Sources/CHDMAN/Resources/AppIcon.icns "$APP/Resources/AppIcon.icns"
          codesign --force --sign - RetroForge.app
          ditto -c -k --keepParent RetroForge.app RetroForge.app.zip

      # 5. Create a GitHub Release with the zip attached
      - name: Create Release
        uses: softprops/action-gh-release@v3
        with:
          files: RetroForge.app.zip
          generate_release_notes: true
